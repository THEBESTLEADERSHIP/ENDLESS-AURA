-- [[ 🥷 ENDLESS STAND NINJA - V39.5 👑 ]] --
-- 🔥 SCRIPT BY: NAMELESS 😎
-- 🎯 ANTI-VOID + ANTI-KILL + PERFECT STAND (VOID ATTACH BYPASS)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local TextChatService = game:GetService("TextChatService")
local StarterGui = game:GetService("StarterGui")
local player = Players.LocalPlayer

local MASTER_ACC = "Nameless82872262"
local CORRECT_KEY = "WHAT IS AURA"
local CLICK_SOUND = "107406973902030"
local LOGO_IMAGE = "rbxassetid://129299711445020"
local LOAD_SOUND = "129584950954499"
if not _G.AdminLogs then _G.AdminLogs = {} end

local voidYLevel = -99999999
local safePosition = Vector3.new(0, 50, 0)

local customAnimId = "121803583995185"
local customTrack = nil

local function toggleCustomEmote(shouldPlay)
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end
    local animator = hum:FindFirstChildOfClass("Animator") or hum:WaitForChild("Animator")
    local animateScript = char:FindFirstChild("Animate")

    if shouldPlay then
        if animateScript then animateScript.Disabled = true end
        for _, t in pairs(animator:GetPlayingAnimationTracks()) do
            t:Stop(0.1)
        end
        local success, objects = pcall(function() 
            return game:GetObjects("rbxassetid://" .. customAnimId) 
        end)
        local animation
        if success and objects[1] and objects[1]:IsA("Animation") then
            animation = objects[1]
        else
            animation = Instance.new("Animation")
            animation.AnimationId = "rbxassetid://" .. customAnimId
        end
        customTrack = animator:LoadAnimation(animation)
        customTrack.Priority = Enum.AnimationPriority.Action4
        customTrack.Looped = true
        customTrack:Play()
        print("Emote Started with High Priority")
    else
        if customTrack then
            customTrack:Stop(0.1)
            customTrack = nil
        end
        if animateScript then animateScript.Disabled = false end
    end
end

if CoreGui:FindFirstChild("EndlessAura_Final") then
    CoreGui.EndlessAura_Final:Destroy()
end

local sg = Instance.new("ScreenGui", CoreGui); sg.Name = "EndlessAura_Final"
local DarkRed = Color3.fromRGB(150, 0, 0)
local Black, White, Green = Color3.fromRGB(0, 0, 0), Color3.fromRGB(255, 255, 255), Color3.fromRGB(0, 255, 0)

local function SendPublicChat(msg)
    task.spawn(function()
        if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local channel = TextChatService:FindFirstChild("TextChannels") and TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if channel then channel:SendAsync(msg) end
        end
        local sayRemote = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest")
        if sayRemote then sayRemote:FireServer(msg, "All") end
    end)
end

local function playS(id)
    local s = Instance.new("Sound", workspace)
    s.SoundId = "rbxassetid://"..(id or CLICK_SOUND)
    s.Volume = 2
    s:Play()
    task.spawn(function()
        s.Ended:Wait()
        s:Destroy()
    end)
end

local function playKeySuccess()
    local s = Instance.new("Sound", workspace)
    s.SoundId = "rbxassetid://123071085988261"
    s.Volume = 0.8
    s:Play()
    task.spawn(function()
        s.Ended:Wait()
        s:Destroy()
    end)
end

local function makeDraggable(f)
    local d, di, ds, sp;
    f.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then d = true; ds = i.Position; sp = f.Position end
    end)
    f.InputChanged:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseMovement then di = i end
    end)
    RunService.RenderStepped:Connect(function()
        if d and di then
            local delta = di.Position - ds; f.Position = UDim2.new(sp.X.Scale, sp.X.Offset + delta.X, sp.Y.Scale, sp.Y.Offset + delta.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then d = false end
    end)
end

local function godModeAttach()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hum then
        hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
        if hum.Health and hum.Health < 99 then hum.Health = 100 end 
        if hum:GetState() == Enum.HumanoidStateType.Dead then hum:ChangeState(Enum.HumanoidStateType.Running) end
    end
    if hrp then
        hrp.Velocity = Vector3.new(0, 0, 0)
        hrp.RotVelocity = Vector3.new(0, 0, 0)
        hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
    end
end

local function SafeMoveTo(targetCFrame)
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hrp then
        godModeAttach()
        hrp.CFrame = targetCFrame
    end
end

-- KEY SYSTEM
local KF = Instance.new("Frame", sg); KF.Size = UDim2.new(0, 340, 0, 440); KF.Position = UDim2.new(0.5, -170, 0.5, -220); KF.BackgroundColor3 = Black; KF.BackgroundTransparency = 0.2; makeDraggable(KF)
Instance.new("UICorner", KF); local KS = Instance.new("UIStroke", KF); KS.Thickness = 3; KS.Color = DarkRed

local KeyLogo = Instance.new("ImageLabel", KF); KeyLogo.Size = UDim2.new(0, 40, 0, 40); KeyLogo.Position = UDim2.new(0.5, -20, 0, 10); KeyLogo.BackgroundTransparency = 1; KeyLogo.Image = LOGO_IMAGE

local CloseKey = Instance.new("TextButton", KF); CloseKey.Size = UDim2.new(0,25,0,25); CloseKey.Position = UDim2.new(1,-30,0,5); CloseKey.Text = "X"; CloseKey.BackgroundColor3 = DarkRed; CloseKey.TextColor3 = White; Instance.new("UICorner", CloseKey)
CloseKey.MouseButton1Click:Connect(function()
    playS(CLICK_SOUND)
    sg:Destroy()
end)

local Title = Instance.new("TextLabel", KF); Title.Text = "ENDLESS AURA"; Title.Size = UDim2.new(1,0,0,40); Title.Position = UDim2.new(0,0,0,50); Title.TextColor3 = DarkRed; Title.TextSize = 22; Title.Font = Enum.Font.GothamBlack; Title.BackgroundTransparency = 1
local Bio = Instance.new("TextLabel", KF); Bio.Text = "SCRIPT BY NAMELESS 👑💀\n\nKEY CHAIYE TO NAMELESS SE MANG\nKISMAT HOGI TO DEDEGA 😂"; Bio.Position = UDim2.new(0,0,0,95); Bio.Size = UDim2.new(1,0,0,60); Bio.TextColor3 = White; Bio.TextSize = 12; Bio.BackgroundTransparency = 1
local Insta = Instance.new("TextLabel", KF); Insta.Text = "INSTAGRAM ID - ea_nameless\nFOLLOW KARLO WRNA ID BAN 💀"; Insta.Position = UDim2.new(0,0,0,165); Insta.Size = UDim2.new(1,0,0,40); Insta.TextColor3 = White; Insta.TextSize = 13; Insta.Font = Enum.Font.GothamBold; Insta.BackgroundTransparency = 1

task.spawn(function()
    while task.wait(0.5) do Insta.TextColor3 = (Insta.TextColor3 == DarkRed and White or DarkRed) end
end)

local KeyBox = Instance.new("TextBox", KF); KeyBox.Size = UDim2.new(0, 240, 0, 45); KeyBox.Position = UDim2.new(0.5, -120, 0, 230); KeyBox.BackgroundColor3 = Color3.fromRGB(20,20,20); KeyBox.PlaceholderText = "KEY DALO..."; KeyBox.TextColor3 = DarkRed; KeyBox.Font = Enum.Font.GothamBold; Instance.new("UICorner", KeyBox)
local Verify = Instance.new("TextButton", KF); Verify.Size = UDim2.new(0, 140, 0, 45); Verify.Position = UDim2.new(0.5, -70, 0, 310); Verify.BackgroundColor3 = White; Verify.Text = "VERIFY"; Verify.TextColor3 = Black; Verify.Font = Enum.Font.GothamBold; Instance.new("UICorner", Verify)

-- COPY NINJA LOGIC
local copyNinjaActive = false
local targetPlayer = nil
local currentTracks = {}
local myAnimationTracks = {}
local copyLoopConnection = nil

local function clearMyTracks()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end
    for _, track in pairs(hum:GetPlayingAnimationTracks()) do
        track:Stop()
    end
    myAnimationTracks = {}
end

local function getTarget()
    if not targetPlayer then return nil end
    return targetPlayer
end

local function copyAnimationTrack(targetTrack, myHum)
    local animId = targetTrack.Animation.AnimationId
    if not animId or animId == "" then return nil end
    local newAnim = Instance.new("Animation")
    newAnim.AnimationId = animId
    local newTrack = myHum:LoadAnimation(newAnim)
    newTrack.Priority = Enum.AnimationPriority.Action4
    newTrack.TimePosition = targetTrack.TimePosition
    if targetTrack.Speed and targetTrack.Speed ~= 1 then
        newTrack:AdjustSpeed(targetTrack.Speed)
    end
    return newTrack
end

local function copyAnimations()
    local target = getTarget()
    if not target then return end
    local tChar = target.Character
    local myChar = player.Character
    if not tChar or not myChar then return end
    local tHum = tChar:FindFirstChild("Humanoid")
    local myHum = myChar:FindFirstChild("Humanoid")
    if not tHum or not myHum then return end
    clearMyTracks()
    currentTracks = {}
    myAnimationTracks = {}
    local targetTracks = tHum:GetPlayingAnimationTracks()
    for _, track in pairs(targetTracks) do
        if track.WeightCurrent > 0.1 then
            local newTrack = copyAnimationTrack(track, myHum)
            if newTrack then
                newTrack:Play()
                table.insert(myAnimationTracks, newTrack)
                table.insert(currentTracks, {
                    targetTrack = track,
                    myTrack = newTrack,
                    animId = track.Animation.AnimationId
                })
            end
        end
    end
    for _, tool in pairs(tChar:GetDescendants()) do
        if tool:IsA("Tool") or tool:IsA("HopperBin") then
            for _, animTrack in pairs(tool:GetDescendants()) do
                if animTrack:IsA("Animation") and animTrack.AnimationId then
                    local newAnim = Instance.new("Animation")
                    newAnim.AnimationId = animTrack.AnimationId
                    local newTrack = myHum:LoadAnimation(newAnim)
                    newTrack.Priority = Enum.AnimationPriority.Action4
                    newTrack:Play()
                    table.insert(myAnimationTracks, newTrack)
                end
            end
        end
    end
end

local function updateCopyLoop()
    if not copyNinjaActive then return end
    local target = getTarget()
    if not target then return end
    local tChar = target.Character
    local myChar = player.Character
    if not tChar or not myChar then return end
    local tHum = tChar:FindFirstChild("Humanoid")
    local myHum = myChar:FindFirstChild("Humanoid")
    if not tHum or not myHum then return end
    local currentAnimIds = {}
    local targetTracks = {}
    for _, track in pairs(tHum:GetPlayingAnimationTracks()) do
        if track.WeightCurrent > 0.1 then
            local id = track.Animation.AnimationId
            if id and id ~= "" then
                currentAnimIds[id] = true
                table.insert(targetTracks, track)
            end
        end
    end
    for i = #currentTracks, 1, -1 do
        local ct = currentTracks[i]
        if not currentAnimIds[ct.animId] then
            if ct.myTrack then
                ct.myTrack:Stop()
            end
            table.remove(currentTracks, i)
        end
    end
    for _, targetTrack in pairs(targetTracks) do
        local id = targetTrack.Animation.AnimationId
        local found = false
        for _, ct in pairs(currentTracks) do
            if ct.animId == id then
                found = true
                if ct.myTrack then
                    ct.myTrack.TimePosition = targetTrack.TimePosition
                    if not ct.myTrack.IsPlaying then
                        ct.myTrack:Play()
                    end
                end
                break
            end
        end
        if not found then
            local newTrack = copyAnimationTrack(targetTrack, myHum)
            if newTrack then
                newTrack:Play()
                table.insert(currentTracks, {
                    targetTrack = targetTrack,
                    myTrack = newTrack,
                    animId = id
                })
                table.insert(myAnimationTracks, newTrack)
            end
        end
    end
end

local function startCopyLoop()
    if copyLoopConnection then
        copyLoopConnection:Disconnect()
        copyLoopConnection = nil
    end
    copyLoopConnection = RunService.RenderStepped:Connect(function()
        updateCopyLoop()
    end)
end

local function stopCopyLoop()
    if copyLoopConnection then
        copyLoopConnection:Disconnect()
        copyLoopConnection = nil
    end
    copyNinjaActive = false
    targetPlayer = nil
    clearMyTracks()
    currentTracks = {}
end

-- CHAT NINJA LOGIC
local chatSyncActive = false
local chatTargetPlayer = nil

local function stopChatSync()
    chatSyncActive = false
    chatTargetPlayer = nil
end

-- STAND NINJA LOGIC
local standMode = false
local standTarget = nil
local standOffset = CFrame.new(0, 0, 0)
local standModeType = 0
local standLookTarget = false
local standLoopConnection = nil

local function updateStandLoop()
    if not standMode then return end
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    godModeAttach()
    if not standTarget then return end
    local tChar = standTarget.Character
    if not tChar then return end
    local tHrp = tChar:FindFirstChild("HumanoidRootPart")
    if not tHrp then return end
    if standLookTarget then
        local targetPos = tHrp.Position
        local desiredPos = (tHrp.CFrame * standOffset).Position
        hrp.CFrame = CFrame.new(desiredPos, Vector3.new(targetPos.X, desiredPos.Y, targetPos.Z))
    else
        hrp.CFrame = tHrp.CFrame * standOffset
    end
    local hum = char:FindFirstChild("Humanoid")
    if hum then hum.Sit = false end
end

local function startStandLoop()
    if standLoopConnection then
        standLoopConnection:Disconnect()
        standLoopConnection = nil
    end
    standLoopConnection = RunService.RenderStepped:Connect(function()
        updateStandLoop()
    end)
end

local function stopStandLoop()
    if standLoopConnection then
        standLoopConnection:Disconnect()
        standLoopConnection = nil
    end
    standMode = false
    standTarget = nil
    standModeType = 0
    local char = player.Character
    if char and char:FindFirstChild("Humanoid") then
        char.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, true)
    end
end

-- GLOBAL TOGGLE TRACKING
local buttonStates = {}
local function resetAllButtons()
    for _, btn in pairs(buttonStates) do
        if btn and btn:IsA("TextButton") then
            btn:SetAttribute("T", false)
            btn.BackgroundColor3 = White
            btn.TextColor3 = Black
        end
    end
    _G.Loop = false
    _G.Fly = false
    _G.InfJump = false
    _G.StrongView = false
    _G.TargetESP = false
    _G.TargetLook = false
    stopCopyLoop()
    stopChatSync()
    stopStandLoop()
    toggleCustomEmote(false)
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.Sit = false
    end
    if workspace.CurrentCamera and player.Character and player.Character:FindFirstChild("Humanoid") then
        workspace.CurrentCamera.CameraSubject = player.Character.Humanoid
    end
    -- Reset stand button if exists
    for _, btn in pairs(buttonStates) do
        if btn.Name == "StandBtn" then
            standModeIdx = 0
            standMode = false
            standTarget = nil
            stopStandLoop()
            btn.Text = "STAND: OFF 🛑"
            btn.BackgroundColor3 = White
            btn.TextColor3 = Black
        end
    end
end

-- MAIN MENU
local function Initialize()
    local entry = player.Name .. " (" .. player.UserId .. ")"
    local alreadyLogged = false
    for _, v in pairs(_G.AdminLogs) do if v == entry then alreadyLogged = true break end end
    if not alreadyLogged then table.insert(_G.AdminLogs, entry) end
    
    task.spawn(function()
        while task.wait(60) do print("💿 SCRIPTED BY NAMELESS💀👑") end
    end)
    
    local Main = Instance.new("Frame", sg); Main.Size = UDim2.new(0, 700, 0, 500); Main.Position = UDim2.new(0.5, -350, 0.5, -250); Main.BackgroundColor3 = Black; Main.BackgroundTransparency = 0.3; makeDraggable(Main)
    Instance.new("UICorner", Main); local MS = Instance.new("UIStroke", Main); MS.Thickness = 2; MS.Color = DarkRed
    
    local MainLogo = Instance.new("ImageLabel", Main); MainLogo.Size = UDim2.new(0, 28, 0, 28); MainLogo.Position = UDim2.new(0, 10, 0, 6); MainLogo.BackgroundTransparency = 1; MainLogo.Image = LOGO_IMAGE
    
    local LogoText = Instance.new("TextLabel", Main); LogoText.Size = UDim2.new(0, 200, 0, 28); LogoText.Position = UDim2.new(0, 43, 0, 5); LogoText.BackgroundTransparency = 1
    LogoText.Text = "Eɴᴅʟᴇss Aᴜʀᴀ"; LogoText.TextColor3 = DarkRed; LogoText.TextSize = 20; LogoText.Font = Enum.Font.GothamBlack; LogoText.TextXAlignment = Enum.TextXAlignment.Left
    
    local TopLine = Instance.new("Frame", Main); TopLine.Size = UDim2.new(1, -20, 0, 2); TopLine.Position = UDim2.new(0, 10, 0, 38)
    TopLine.BackgroundColor3 = DarkRed; TopLine.BorderSizePixel = 0
    
    -- LEFT PANEL - Target Headshot
    local LeftPanel = Instance.new("Frame", Main); LeftPanel.Size = UDim2.new(0, 220, 1, -50); LeftPanel.Position = UDim2.new(0, 10, 0, 45); LeftPanel.BackgroundTransparency = 1
    
    local HeadshotFrame = Instance.new("Frame", LeftPanel); HeadshotFrame.Size = UDim2.new(1, 0, 0, 220); HeadshotFrame.BackgroundColor3 = Color3.fromRGB(10,10,10); Instance.new("UICorner", HeadshotFrame)
    local HeadshotImage = Instance.new("ImageLabel", HeadshotFrame); HeadshotImage.Size = UDim2.new(1, -20, 1, -20); HeadshotImage.Position = UDim2.new(0, 10, 0, 10); HeadshotImage.BackgroundTransparency = 1; HeadshotImage.Image = "rbxassetid://1233949370"
    local HeadshotLabel = Instance.new("TextLabel", HeadshotFrame); HeadshotLabel.Size = UDim2.new(1, 0, 0, 20); HeadshotLabel.Position = UDim2.new(0, 0, 1, -25); HeadshotLabel.BackgroundTransparency = 1; HeadshotLabel.Text = "TARGET HEADSHOT"; HeadshotLabel.TextColor3 = DarkRed; HeadshotLabel.TextSize = 12; HeadshotLabel.Font = Enum.Font.GothamBold
    
    local TargetTextBox = Instance.new("TextBox", LeftPanel); TargetTextBox.Size = UDim2.new(1, 0, 0, 30); TargetTextBox.Position = UDim2.new(0, 0, 0, 230); TargetTextBox.PlaceholderText = "TARGET NAME..."; TargetTextBox.BackgroundColor3 = Color3.fromRGB(20,20,20); TargetTextBox.TextColor3 = White; Instance.new("UICorner", TargetTextBox)
    local function getT() local n = TargetTextBox.Text:lower() for _,v in pairs(Players:GetPlayers()) do if v ~= player and (v.Name:lower():find(n) or v.DisplayName:lower():find(n)) then return v end end end
    
    -- Update Headshot in real-time
    local headshotConnection
    headshotConnection = RunService.RenderStepped:Connect(function()
        local t = getT()
        if t and t.Character and t.Character:FindFirstChild("Head") then
            local head = t.Character.Head
            local thumb = Players:GetUserThumbnailAsync(t.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
            if thumb then HeadshotImage.Image = thumb end
            HeadshotLabel.Text = t.Name .. " | " .. t.DisplayName
            HeadshotLabel.TextColor3 = Green
        else
            HeadshotImage.Image = "rbxassetid://1233949370"
            HeadshotLabel.Text = "NO TARGET"
            HeadshotLabel.TextColor3 = White
        end
    end)
    
    -- RIGHT PANEL - Target Info + TAXT BOX
    local RightPanel = Instance.new("Frame", Main); RightPanel.Size = UDim2.new(0, 230, 1, -50); RightPanel.Position = UDim2.new(1, -240, 0, 45); RightPanel.BackgroundTransparency = 1
    
    local InfoFrame = Instance.new("Frame", RightPanel); InfoFrame.Size = UDim2.new(1, 0, 0, 150); InfoFrame.BackgroundColor3 = Color3.fromRGB(10,10,10); Instance.new("UICorner", InfoFrame)
    local InfoText = Instance.new("TextLabel", InfoFrame); InfoText.Size = UDim2.new(1, -10, 1, -10); InfoText.Position = UDim2.new(0, 5, 0, 5); InfoText.BackgroundTransparency = 1; InfoText.TextColor3 = White; InfoText.TextSize = 10; InfoText.Font = Enum.Font.Gotham; InfoText.TextXAlignment = Enum.TextXAlignment.Left; InfoText.TextYAlignment = Enum.TextYAlignment.Top
    InfoText.Text = "TARGET INFO:\n\nName: -\nDisplay: -\nUserID: -\nDistance: -\nHealth: -\nStatus: -"
    
    -- TAXT BOX
    local TaxtBox = Instance.new("Frame", RightPanel); TaxtBox.Size = UDim2.new(1, 0, 0, 80); TaxtBox.Position = UDim2.new(0, 0, 0, 160); TaxtBox.BackgroundColor3 = Color3.fromRGB(10,10,10); Instance.new("UICorner", TaxtBox)
    local TaxtLabel = Instance.new("TextLabel", TaxtBox); TaxtLabel.Size = UDim2.new(1, -10, 1, -10); TaxtLabel.Position = UDim2.new(0, 5, 0, 5); TaxtLabel.BackgroundTransparency = 1; TaxtLabel.TextColor3 = White; TaxtLabel.TextSize = 11; TaxtLabel.Font = Enum.Font.GothamBold; TaxtLabel.Text = "TAXT BOX\n\n@EA_FAN\n@name"
    
    -- Update Target Info in real-time
    local infoConnection
    infoConnection = RunService.RenderStepped:Connect(function()
        local t = getT()
        if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") and t.Character:FindFirstChild("Humanoid") then
            local hrp = t.Character.HumanoidRootPart
            local hum = t.Character.Humanoid
            local myHrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            local dist = myHrp and (hrp.Position - myHrp.Position).Magnitude or 0
            local status = hum:GetState() == Enum.HumanoidStateType.Dead and "DEAD 💀" or "ALIVE ✅"
            InfoText.Text = string.format("TARGET INFO:\n\nName: %s\nDisplay: %s\nUserID: %d\nDistance: %.1f studs\nHealth: %.0f/100\nStatus: %s", 
                t.Name, t.DisplayName, t.UserId, dist, hum.Health, status)
            InfoText.TextColor3 = hum.Health > 50 and Green or DarkRed
        else
            InfoText.Text = "TARGET INFO:\n\nName: -\nDisplay: -\nUserID: -\nDistance: -\nHealth: -\nStatus: -"
            InfoText.TextColor3 = White
        end
    end)
    
    -- CENTER PANEL - All Buttons
    local CenterPanel = Instance.new("Frame", Main); CenterPanel.Size = UDim2.new(1, -480, 1, -50); CenterPanel.Position = UDim2.new(0, 230, 0, 45); CenterPanel.BackgroundTransparency = 1
    
    local ButtonGrid = Instance.new("Frame", CenterPanel); ButtonGrid.Size = UDim2.new(1, 0, 1, -50); ButtonGrid.BackgroundTransparency = 1
    local GridLayout = Instance.new("UIGridLayout", ButtonGrid); GridLayout.CellSize = UDim2.new(0.45, 0, 0, 32); GridLayout.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
    
    -- STOP ALL Button (Big & Centered at bottom)
    local StopAllFrame = Instance.new("Frame", CenterPanel); StopAllFrame.Size = UDim2.new(1, 0, 0, 40); StopAllFrame.Position = UDim2.new(0, 0, 1, -40); StopAllFrame.BackgroundTransparency = 1
    
    local StopAllBtn = Instance.new("TextButton", StopAllFrame); StopAllBtn.Size = UDim2.new(0.4, 0, 1, 0); StopAllBtn.Position = UDim2.new(0.3, 0, 0, 0); StopAllBtn.BackgroundColor3 = DarkRed; StopAllBtn.Text = "STOP ALL 🛑"; StopAllBtn.TextColor3 = White; StopAllBtn.Font = Enum.Font.GothamBold; StopAllBtn.TextSize = 14; Instance.new("UICorner", StopAllBtn)
    StopAllBtn.MouseButton1Click:Connect(function()
        playS(CLICK_SOUND)
        resetAllButtons()
    end)
    
    -- Helper to create buttons
    local function createButton(text, parent, func, isLoad)
        local b = Instance.new("TextButton", parent)
        b.BackgroundColor3 = isLoad and DarkRed or White
        b.Text = string.upper(text)
        b.TextColor3 = isLoad and White or Black
        b.Font = Enum.Font.GothamBold
        b.TextSize = 8.5
        Instance.new("UICorner", b)
        buttonStates[b] = b
        b.MouseButton1Click:Connect(function()
            playS(CLICK_SOUND)
            if not isLoad then 
                b:SetAttribute("T", not b:GetAttribute("T"))
                b.BackgroundColor3 = b:GetAttribute("T") and DarkRed or White
                b.TextColor3 = b:GetAttribute("T") and White or Black
            end
            func(b)
        end)
        return b
    end
    
    -- TAB 1: INFO
    local InfoPanel = Instance.new("Frame", CenterPanel); InfoPanel.Size = UDim2.new(1, 0, 1, -40); InfoPanel.BackgroundTransparency = 1; InfoPanel.Visible = true
    
    local InfoLogo2 = Instance.new("ImageLabel", InfoPanel); InfoLogo2.Size = UDim2.new(0, 65, 0, 65); InfoLogo2.Position = UDim2.new(0.5, -32.5, 0, 10); InfoLogo2.BackgroundTransparency = 1; InfoLogo2.Image = LOGO_IMAGE
    local InfoText2 = Instance.new("TextLabel", InfoPanel); InfoText2.Size = UDim2.new(1,0,1,-85); InfoText2.Position = UDim2.new(0, 0, 0, 80); InfoText2.BackgroundTransparency = 1; InfoText2.TextColor3 = White; InfoText2.TextSize = 10; InfoText2.Font = Enum.Font.GothamBold; InfoText2.TextXAlignment = "Left"; InfoText2.TextYAlignment = "Top"
    InfoText2.Text = [[SCRIPT NAME - ENDLESS AURA
__________________________________________
SCRIPT BY: NAMELESS 👑💀
INSTAGRAM ID: ea_nameless

FAMOUS FATHER: NAMELESS 💀
THE-LEADER OF [ENDLESS AURA]
THIS SCRIPT ONLY FOR ENDLESS AURA MEMBERS 👑💝
__________________________________________
OR SABSE ACHI BAAT EGO EMPIRE APNA BETA HAI]]
    
    -- TROLL Panel
    local TrollPanel = Instance.new("Frame", CenterPanel); TrollPanel.Size = UDim2.new(1, 0, 1, -40); TrollPanel.BackgroundTransparency = 1; TrollPanel.Visible = false
    local TrollGrid = Instance.new("UIGridLayout", TrollPanel); TrollGrid.CellSize = UDim2.new(0.45, 0, 0, 32); TrollGrid.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
    
    createButton("👁️ VIEW", TrollPanel, function(b)
        _G.StrongView = b:GetAttribute("T")
        task.spawn(function()
            while _G.StrongView do
                local t = getT()
                if t and t.Character and t.Character:FindFirstChild("Humanoid") then
                    if workspace.CurrentCamera.CameraSubject ~= t.Character.Humanoid then
                        workspace.CurrentCamera.CameraSubject = t.Character.Humanoid
                    end
                end
                task.wait(0.01)
            end
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                workspace.CurrentCamera.CameraSubject = player.Character.Humanoid
            end
        end)
    end)
    
    createButton("🔴 TARGET ESP", TrollPanel, function(b)
        _G.TargetESP = b:GetAttribute("T")
        task.spawn(function()
            local currentHighlight, currentBillboard
            local function cleanup()
                if currentHighlight then currentHighlight:Destroy(); currentHighlight = nil end
                if currentBillboard then currentBillboard:Destroy(); currentBillboard = nil end
            end
            while _G.TargetESP do
                local t = getT()
                if t and t.Character then
                    if not currentHighlight or currentHighlight.Parent ~= t.Character then
                        cleanup()
                        currentHighlight = Instance.new("Highlight")
                        currentHighlight.FillColor = Color3.fromRGB(255, 0, 0)
                        currentHighlight.FillTransparency = 0.6
                        currentHighlight.OutlineColor = Color3.fromRGB(255, 0, 0)
                        currentHighlight.OutlineTransparency = 0
                        currentHighlight.Parent = t.Character
                    end
                    local head = t.Character:FindFirstChild("Head")
                    if head and (not currentBillboard or currentBillboard.Parent ~= head) then
                        if currentBillboard then currentBillboard:Destroy() end
                        currentBillboard = Instance.new("BillboardGui")
                        currentBillboard.Size = UDim2.new(0, 100, 0, 30)
                        currentBillboard.AlwaysOnTop = true
                        currentBillboard.StudsOffset = Vector3.new(0, 2, 0)
                        local tl = Instance.new("TextLabel", currentBillboard)
                        tl.Size = UDim2.new(1, 0, 1, 0)
                        tl.BackgroundTransparency = 1
                        tl.Text = "ᴛᴀʀɢᴇᴛ"
                        tl.TextColor3 = Color3.fromRGB(255, 0, 0)
                        tl.TextSize = 16
                        tl.Font = Enum.Font.GothamBlack
                        local stroke = Instance.new("UIStroke", tl)
                        stroke.Thickness = 3
                        stroke.Color = Color3.fromRGB(0, 0, 0)
                        currentBillboard.Parent = head
                    end
                else
                    cleanup()
                end
                task.wait(0.01)
            end
            cleanup()
        end)
    end)
    
    createButton("👁️ TARGET LOOK", TrollPanel, function(b)
        _G.TargetLook = b:GetAttribute("T")
        local lookConnection
        if _G.TargetLook then
            lookConnection = RunService.RenderStepped:Connect(function()
                if not _G.TargetLook then if lookConnection then lookConnection:Disconnect() end return end
                local t = getT()
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if t and t.Character and hrp then
                    local targetHrp = t.Character:FindFirstChild("HumanoidRootPart")
                    if targetHrp then
                        hrp.RotVelocity = Vector3.zero
                        local lookPos = Vector3.new(targetHrp.Position.X, hrp.Position.Y, targetHrp.Position.Z)
                        if (lookPos - hrp.Position).Magnitude > 0.1 then
                            hrp.CFrame = CFrame.lookAt(hrp.Position, lookPos)
                        end
                    end
                end
            end)
        else
            if lookConnection then lookConnection:Disconnect() end
        end
    end)
    
    createButton("🚀 GOTO", TrollPanel, function()
        local t = getT()
        if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") then
            SafeMoveTo(t.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 2))
        end
    end, true)
    
    createButton("🍆 BANG", TrollPanel, function(b)
        _G.Loop = b:GetAttribute("T")
        toggleCustomEmote(_G.Loop)
        task.spawn(function()
            while _G.Loop do
                local t = getT()
                if t and t.Character and t.Character:FindFirstChild("HumanoidRootPart") then
                    SafeMoveTo(t.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 1.1 + math.sin(tick()*20)*0.55))
                else
                    godModeAttach()
                end
                RunService.RenderStepped:Wait()
            end
            local char = player.Character
            if char and char:FindFirstChild("Humanoid") then char.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, true) end
        end)
    end)
    
    createButton("🧒 HEADSIT", TrollPanel, function(b)
        _G.Loop = b:GetAttribute("T")
        task.spawn(function()
            while _G.Loop do
                local t = getT()
                if t and t.Character and t.Character:FindFirstChild("Head") then
                    SafeMoveTo(t.Character.Head.CFrame * CFrame.new(0, 0.45 + (math.sin(tick()*10)*0.05), 0))
                    if player.Character:FindFirstChild("Humanoid") then player.Character.Humanoid.Sit = true end
                else
                    godModeAttach()
                end
                RunService.RenderStepped:Wait()
            end
            local char = player.Character
            if char and char:FindFirstChild("Humanoid") then char.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, true) end
        end)
    end)
    
    createButton("🦒 FRONT HEADSIT", TrollPanel, function(b)
        _G.Loop = b:GetAttribute("T")
        toggleCustomEmote(_G.Loop)
        task.spawn(function()
            while _G.Loop do
                local t = getT()
                if t and t.Character and t.Character:FindFirstChild("Head") then
                    SafeMoveTo(t.Character.Head.CFrame * CFrame.new(0, 0.45 + (math.sin(tick()*10)*0.05), -0.5) * CFrame.Angles(0, math.pi, 0))
                    if player.Character:FindFirstChild("Humanoid") then player.Character.Humanoid.Sit = true end
                else
                    godModeAttach()
                end
                RunService.RenderStepped:Wait()
            end
            local char = player.Character
            if char and char:FindFirstChild("Humanoid") then char.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, true) end
        end)
    end)
    
    createButton("COPY ANIM👤", TrollPanel, function(b)
        local isActive = b:GetAttribute("T")
        if isActive then
            targetPlayer = getT()
            if targetPlayer then
                copyNinjaActive = true
                copyAnimations()
                startCopyLoop()
            else
                b:SetAttribute("T", false)
                b.BackgroundColor3 = White
                b.TextColor3 = Black
            end
        else
            stopCopyLoop()
        end
    end)
    
    createButton("COPY CHAT💬", TrollPanel, function(b)
        local isActive = b:GetAttribute("T")
        if isActive then
            chatTargetPlayer = getT()
            if chatTargetPlayer then
                chatSyncActive = true
            else
                b:SetAttribute("T", false)
                b.BackgroundColor3 = White
                b.TextColor3 = Black
            end
        else
            stopChatSync()
        end
    end)
    
    -- STAND BUTTON (Special)
    local standBtn = createButton("STAND: OFF 🛑", TrollPanel, function(b)
        if standModeIdx == 0 then
            standTarget = getT()
            if not standTarget then
                standBtn.Text = "NOT FOUND!"
                task.wait(0.5)
                standBtn.Text = "STAND: OFF 🛑"
                return
            end
        end
        standModeIdx = (standModeIdx + 1) % 6
        if standModeIdx == 1 then
            standMode = true; standLookTarget = false; standOffset = CFrame.new(0, 0, -5); standBtn.Text = "FRONT ⬆️"; standBtn.BackgroundColor3 = DarkRed; standBtn.TextColor3 = White; startStandLoop()
        elseif standModeIdx == 2 then
            standMode = true; standLookTarget = false; standOffset = CFrame.new(0, 0, 5); standBtn.Text = "BACK ⬇️"; standBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0); standBtn.TextColor3 = White; startStandLoop()
        elseif standModeIdx == 3 then
            standMode = true; standLookTarget = false; standOffset = CFrame.new(-5, 0, 0); standBtn.Text = "LEFT ⬅️"; standBtn.BackgroundColor3 = Color3.fromRGB(100, 0, 0); standBtn.TextColor3 = White; startStandLoop()
        elseif standModeIdx == 4 then
            standMode = true; standLookTarget = false; standOffset = CFrame.new(5, 0, 0); standBtn.Text = "RIGHT ➡️"; standBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 0); standBtn.TextColor3 = White; startStandLoop()
        elseif standModeIdx == 5 then
            standMode = true; standLookTarget = true; standOffset = CFrame.new(0, 0, -5); standBtn.Text = "FF 👀"; standBtn.BackgroundColor3 = Color3.fromRGB(60, 0, 80); standBtn.TextColor3 = White; startStandLoop()
        elseif standModeIdx == 0 then
            standMode = false; standTarget = nil; standBtn.Text = "STAND: OFF 🛑"; standBtn.BackgroundColor3 = White; standBtn.TextColor3 = Black; stopStandLoop()
        end
    end, false)
    standBtn.Name = "StandBtn"
    
    -- SHIELD Panel
    local ShieldPanel = Instance.new("Frame", CenterPanel); ShieldPanel.Size = UDim2.new(1, 0, 1, -40); ShieldPanel.BackgroundTransparency = 1; ShieldPanel.Visible = false
    local ShieldGrid = Instance.new("UIGridLayout", ShieldPanel); ShieldGrid.CellSize = UDim2.new(0.45, 0, 0, 32); ShieldGrid.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
    
    createButton("🛡️ ANTI VOID", ShieldPanel, function(b) task.spawn(function() while b:GetAttribute("T") do if not _G.Loop and not standMode then local hrp = player.Character:FindFirstChild("HumanoidRootPart") if hrp and hrp.Position.Y < voidYLevel then hrp.CFrame = CFrame.new(safePosition) end end task.wait(0.01) end end) end)
    createButton("ANTI VOID V1🛡️", ShieldPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/ANTI%20VOID%20V1"))() end, true)
    createButton("ANTI BANG🛡️", ShieldPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/d4r8JDXz"))() end, true)
    createButton("🛡️ ANTI SIT", ShieldPanel, function(b) task.spawn(function() while b:GetAttribute("T") do if not _G.Loop then player.Character.Humanoid.Sit = false end task.wait() end end) end)
    createButton("🛡️ ANTI AFK", ShieldPanel, function() game:GetService("VirtualUser"):ClickButton2(Vector2.new()) end, true)
    createButton("🛡️ ANTI FLING", ShieldPanel, function(b) task.spawn(function() while b:GetAttribute("T") do player.Character.HumanoidRootPart.Velocity = Vector3.zero; player.Character.HumanoidRootPart.RotVelocity = Vector3.zero task.wait() end end) end)
    
    -- SPAM Panel
    local SpamPanel = Instance.new("Frame", CenterPanel); SpamPanel.Size = UDim2.new(1, 0, 1, -40); SpamPanel.BackgroundTransparency = 1; SpamPanel.Visible = false
    local SpamGrid = Instance.new("UIGridLayout", SpamPanel); SpamGrid.CellSize = UDim2.new(0.45, 0, 0, 32); SpamGrid.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
    
    createButton("SHIRO×BHUMIKA🖤", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/9S2h4aDC"))() end, true)
    createButton("NAMELESS SPAMMER💢", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/bAdrcuP8"))() end, true)
    createButton("BHUMIKA SPAMMER 💖", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/3UfyBuCx"))() end, true)
    createButton("🎵 MUSIC SPAMMER", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/U0NdycvQ"))() end, true)
    createButton("📢 WARD SPAMMER", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/V27NiC4e"))() end, true)
    createButton("💀 @ SPAMMER", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/Gv6RtAmL"))() end, true)
    createButton("CUSTOM SPAMMER💢", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/CUSTOM%20SPAMMER"))() end, true)
    createButton("CUSTOM SPAMMER V1💢", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/CUSTOM%20SPAMMER%20V2"))() end, true)
    createButton("MIDDLE CUSTOM SPAM🌈", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/MIDDLE%20COSTOME%20SPAM"))() end, true)
    createButton("FIRE SPAMMER🔥", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/FIRE%20SPAMMER%F0%9F%94%A5"))() end, true)
    createButton("OCEAN SPAMMER🌊", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/OCEAN%20SPAMMER%F0%9F%8C%8A"))() end, true)
    createButton("GRASS SPAMMER🌲", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/GRASS%20SPAMMER%F0%9F%8C%B2"))() end, true)
    createButton("GORZILLA SPAMMER🦍", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/84c5d1ed122de825817a657502382f358a21efaa/GORZILLA%20SPAMMER%F0%9F%A6%8D"))() end, true)
    createButton("FROG SPAMMER🐸", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/FROG%20SPAMMER%F0%9F%90%B8"))() end, true)
    createButton("JHATU EMPIRE SPAM😂", SpamPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/m3wpKv3d"))() end, true)
    createButton("RANDI SPAM😂", SpamPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/RANDI%20SPAMMER"))() end, true)
    
    -- OTHER Panel
    local OtherPanel = Instance.new("Frame", CenterPanel); OtherPanel.Size = UDim2.new(1, 0, 1, -40); OtherPanel.BackgroundTransparency = 1; OtherPanel.Visible = false
    local OtherGrid = Instance.new("UIGridLayout", OtherPanel); OtherGrid.CellSize = UDim2.new(0.45, 0, 0, 32); OtherGrid.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
    
    createButton("BANG PANEL 💀💢", OtherPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA-TAG/refs/heads/main/BANG%20PANEL%F0%9F%92%80%F0%9F%92%A2"))() end, true)
    createButton("ANTIBANG PANEL 💀🛡️", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/BM0uamrz"))() end, true)
    createButton("⚡ SPEED 300", OtherPanel, function(b) player.Character.Humanoid.WalkSpeed = b:GetAttribute("T") and 300 or 16 end)
    createButton("🆙 FLY JUMP", OtherPanel, function(b) _G.InfJump = b:GetAttribute("T") UserInputService.JumpRequest:Connect(function() if _G.InfJump then player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end end) end)
    createButton("🌀 SPIN 300", OtherPanel, function(b) task.spawn(function() while b:GetAttribute("T") do player.Character.HumanoidRootPart.CFrame *= CFrame.Angles(0, math.rad(20), 0) task.wait() end end) end)
    createButton("FLY 🦅", OtherPanel, function() loadstring(game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA/refs/heads/main/FLY%20%F0%9F%A6%85"))() end, false)
    createButton("GHOST NOCLIP", OtherPanel, function(b) RunService.Stepped:Connect(function() if b:GetAttribute("T") then for _,v in pairs(player.Character:GetChildren()) do if v:IsA("BasePart") then v.CanCollide = false end end end end) end)
    createButton("AIR WALK 🍃", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/tLukyENu"))() end, true)
    createButton("AURA♀️", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/LmxNcQvt"))() end, true)
    createButton("HIDE BUTTONS 🔘", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/DtJPutfe"))() end, true)
    createButton("🥷 COPY NINJA", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/7QTTJFFa"))() end, true)
    createButton("🪓 SKY DESTROYER", OtherPanel, function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-sky-destroyer-V2-AXEZZ-110640"))() end, true)
    createButton("🎭 EMOTE NAME", OtherPanel, function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-7yd7-I-Emote-Script-48024"))() end) end, true)
    createButton("NAMELESS EMOTE👤", OtherPanel, function() loadstring(game:HttpGet("https://pastebin.com/raw/VV3ggiki"))() end, true)
    createButton("🔄 REJOIN", OtherPanel, function() TeleportService:Teleport(game.PlaceId, player) end, true)
    
    -- TABS
    local TabFrame = Instance.new("Frame", CenterPanel); TabFrame.Size = UDim2.new(1, 0, 0, 30); TabFrame.Position = UDim2.new(0, 0, 1, -40); TabFrame.BackgroundTransparency = 1
    local TabLayout = Instance.new("UILayout", TabFrame); TabLayout.FillDirection = Enum.FillDirection.Horizontal; TabLayout.Padding = UDim.new(0, 5); TabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    
    local tabs = {
        {"👑 INFO", InfoPanel},
        {"🌚 TROLL", TrollPanel},
        {"🛡️ SHIELD", ShieldPanel},
        {"👺 SPAM", SpamPanel},
        {"📁 OTHER", OtherPanel}
    }
    
    if player.Name == MASTER_ACC then
        local AdminPanel = Instance.new("Frame", CenterPanel); AdminPanel.Size = UDim2.new(1, 0, 1, -40); AdminPanel.BackgroundTransparency = 1; AdminPanel.Visible = false
        local AdminGrid = Instance.new("UIGridLayout", AdminPanel); AdminGrid.CellSize = UDim2.new(0.45, 0, 0, 32); AdminGrid.CellPadding = UDim2.new(0.05, 0, 0.03, 0)
        local LogL = Instance.new("TextLabel", AdminPanel); LogL.Size = UDim2.new(1,0,1,0); LogL.BackgroundTransparency = 1; LogL.TextColor3 = Green; LogL.TextSize = 8.5; LogL.Font = Enum.Font.Code; LogL.TextXAlignment = "Left"; LogL.TextYAlignment = "Top"
        RunService.Heartbeat:Connect(function()
            local t = "[ MASTER ADMIN ACCESS ]\n--- ACTIVE USERS ---\n"
            for _,v in pairs(_G.AdminLogs) do t = t .. "> " .. v .. "\n" end
            LogL.Text = t
        end)
        table.insert(tabs, {"🛡️ ADMIN", AdminPanel})
    end
    
    for _, tabInfo in pairs(tabs) do
        local tb = Instance.new("TextButton", TabFrame); tb.Size = UDim2.new(0, 0, 1, 0); tb.BackgroundColor3 = Color3.fromRGB(15,15,15); tb.Text = string.upper(tabInfo[1]); tb.TextColor3 = White; tb.Font = Enum.Font.GothamBold; tb.TextSize = 10; Instance.new("UICorner", tb)
        tb.AutoButtonColor = false
        local panel = tabInfo[2]
        tb.MouseButton1Click:Connect(function()
            playS(CLICK_SOUND)
            for _,v in pairs(CenterPanel:GetChildren()) do
                if v:IsA("Frame") and v ~= TabFrame then
                    v.Visible = false
                end
            end
            panel.Visible = true
            for _,v in pairs(TabFrame:GetChildren()) do
                if v:IsA("TextButton") then
                    v.TextColor3 = White
                end
            end
            tb.TextColor3 = DarkRed
        end)
        -- Make buttons fit text
        tb:GetPropertyChangedSignal("TextBounds"):Connect(function()
            tb.Size = UDim2.new(0, tb.TextBounds.X + 20, 1, 0)
        end)
    end
    
    -- Show first tab
    for _,v in pairs(CenterPanel:GetChildren()) do
        if v:IsA("Frame") and v ~= TabFrame then
            v.Visible = false
        end
    end
    InfoPanel.Visible = true
    for _,v in pairs(TabFrame:GetChildren()) do
        if v:IsA("TextButton") then
            v.TextColor3 = White
        end
    end
    TabFrame:GetChildren()[1].TextColor3 = DarkRed
    
    local Logo = Instance.new("ImageButton", sg); Logo.Size = UDim2.new(0, 50, 0, 50); Logo.Position = UDim2.new(0, 10, 0.5, -25)
    Logo.BackgroundTransparency = 1
    Logo.Image = LOGO_IMAGE; makeDraggable(Logo)
    
    Logo.MouseButton1Click:Connect(function()
        playS(CLICK_SOUND)
        Main.Visible = not Main.Visible
    end)
    
    -- CHAT SYNC LISTENER
    TextChatService.MessageReceived:Connect(function(msg)
        if chatSyncActive and chatTargetPlayer and msg.TextSource and msg.TextSource.UserId == chatTargetPlayer.UserId then
            local channel = TextChatService:FindFirstChild("TextChannels") and TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if channel then channel:SendAsync(msg.Text) end
            local sayRemote = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents") and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest")
            if sayRemote then sayRemote:FireServer(msg.Text, "All") end
        end
    end)
end

-- VERIFY SEQUENCE
Verify.MouseButton1Click:Connect(function()
    if KeyBox.Text == CORRECT_KEY then
        playS(CLICK_SOUND)
        playKeySuccess()
        if KF then KF:Destroy() end
        task.spawn(function()
            SendPublicChat("")
            task.wait(5)
            SendPublicChat("..")
            playS(LOAD_SOUND)
            StarterGui:SetCore("SendNotification", {
                Title = "ENDLESS AURA",
                Text = "Loaded...",
                Icon = LOGO_IMAGE,
                Duration = 5
            })
            task.spawn(function()
                local success, err = pcall(function()
                    local response = game:HttpGet("https://raw.githubusercontent.com/THEBESTLEADERSHIP/ENDLESS-AURA-TAG/refs/heads/main/ENDLESS%20AURA%20TAG")
                    local func = loadstring(response)
                    if func then func() end
                end)
            end)
            task.wait(0.2)
            Initialize()
        end)
    else
        playS(CLICK_SOUND)
        KeyBox.Text = ""
        KeyBox.PlaceholderText = "WRONG KEY 😂"
    end
end)
